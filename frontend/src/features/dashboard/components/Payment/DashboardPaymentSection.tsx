'use client';

import PaymentHistory from './PaymentHistory';

export default function DashboardPaymentSection() {
  return (
    <section className="rounded-[36px] bg-white">
      <PaymentHistory />
    </section>
  );
}